[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py migrate", "python manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn Proyecto.wsgi:application --bind 0.0.0.0:$PORT"